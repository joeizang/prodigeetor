# Monoid Font Bundle

Place the Monoid font files in this directory (for example `Monoid-Regular.ttf` and `Monoid-Bold.ttf`).

This project does not ship the font binaries yet. Add your licensed copies here to bundle them with the app.
